#include <stdio.h>
/**
 * main - Starts here
 * Return: Consistently return 0 (Success)
 */

int main(void)
{
	char q;

	int r;

	q = 'a';
	r = 0;
	while
		(r < 10) {
			putchar(r + '0');
			r++;
		}
	while
		(q <= 'f') {
			putchar(q);
			q++;
		}
	putchar('\n');
	return (0);
}
