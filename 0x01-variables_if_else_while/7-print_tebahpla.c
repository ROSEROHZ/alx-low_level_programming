#include <stdio.h>
/**
 * main - Starts here
 * Return: Consistently return 0 (Success)
 */

int main(void)
{
	char q;

	q = 'z';
	while
		(q >= 'a') {
			putchar(q);
			q--;
		}
	putchar('\n');
	return (0);
}
